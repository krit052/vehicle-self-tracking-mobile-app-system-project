<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- ตามตำแหน่งเจ้าของต่อในพื้นหลังแม้ปิดแอป/สลับแอปไปแล้ว (ดู
         lib/services/background_location_service.dart) รันเป็น foreground service ที่มี
         notification ค้างตลอดตอน tracking อยู่ — ต้องประกาศ permission เหล่านี้ให้ครบตาม
         Android 14+ (API 34) ไม่งั้น foreground service type="location" จะ start ไม่ได้ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <application
        android:label="frontend"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- FCM notification messages with no explicit channel_id fall back to this
             channel (created with IMPORTANCE_HIGH in MainActivity) so they show as
             heads-up popups with vibration instead of a silent tray entry. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="high_importance_channel" />
        <!-- flutter_background_service ประกาศ <service> นี้เองใน manifest ของปลั๊กอิน แต่ไม่ใส่
             foregroundServiceType ไว้ (ค่า default = ไม่มี) ส่วนโค้ดเราสั่ง startForeground ด้วย
             type "location" ตอนรันจริง (ดู AndroidForegroundType.location ใน
             background_location_service.dart) — บน Android 14+ (API 34) สอง อย่างนี้ต้องตรงกัน
             ไม่งั้น crash ทันทีตอน service พยายาม start (IllegalArgumentException: foregroundServiceType
             ... is not a subset of ... attribute ... in service element of manifest file) ต้อง
             override ให้ตรงกันตรงนี้ (merge ทับค่าจากปลั๊กอินด้วยชื่อ service เดียวกัน) -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="location" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
